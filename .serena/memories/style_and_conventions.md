Codebase conventions from repo docs: prefer small targeted changes; maintain MV3-safe patterns; avoid dynamic imports in background; keep background handlers stateless and Supabase-backed; use shared message constants/contracts and direct response payload properties (not nested `response.data`). UI uses Preact patterns already present in the repo; follow existing naming and file organization instead of introducing new abstractions casually. Logging should use the project logger rather than stray console calls. OpenSpec is used for non-trivial changes and changes should validate strictly before handoff.