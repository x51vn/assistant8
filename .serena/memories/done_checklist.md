When finishing a task in this repo, run the smallest relevant verification set from: `npm run build`, `npm run test:unit:run`, focused `npm run test:e2e:smoke` or broader Playwright coverage as needed, `npm run check:repo-hygiene`, `npm run check:openspec`, and for OpenSpec changes `openspec validate <change> --strict --no-interactive`. Also preserve MV3 constraints, avoid storing durable user data locally, and ensure any new cross-context messages are reflected in shared message schema/contracts.